"""Self-hosted VerifyDoc server (FastAPI): REST API + web review UI + messaging
bots. Import lazily via :func:`verifydoc.server.app.build_app` — requires the
``server`` extra (``pip install 'verifydoc[server]'``)."""
